import mongoose from 'mongoose';

const sodaSchema = mongoose.Schema({
    title: String,
    message: String,
    creator: String,
    tags: [String],
    selectedFile: String,
    likeCount: {
        type: Number,
        default: 0,
    },
    createdAt: {
        type: Date,
        default: new Date(),
    },
})

var Sodas = mongoose.model('PostMessage', sodaSchema);

export default Sodas;